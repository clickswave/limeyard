# Optional overrides for the vuln_apps manager. Copy to .env if needed.

# How often (seconds) the `docker compose up` monitor loop refreshes status.
# MONITOR_INTERVAL=30

# Pin a specific crAPI image tag instead of :latest.
# VERSION=latest
