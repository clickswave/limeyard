#!/usr/bin/env bash
# Convenience wrapper around the control plane. These are equivalent:
#   ./lime start --all
#   docker compose run --rm limeyard_control start --all
set -euo pipefail
cd "$(dirname "$0")"
exec docker compose run --rm limeyard_control "$@"
