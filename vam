#!/usr/bin/env bash
# Convenience wrapper around the manager. These are equivalent:
#   ./vam start --all
#   docker compose run --rm vuln_apps_manager start --all
set -euo pipefail
cd "$(dirname "$0")"
exec docker compose run --rm vuln_apps_manager "$@"
