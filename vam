#!/usr/bin/env bash
# Deprecated. vuln_apps became limeyard; `vam` became `lime`.
# Kept for one release so existing muscle memory and scripts keep working.
set -euo pipefail
echo "vam is deprecated: vuln_apps is now limeyard, and vam is now lime." >&2
echo "  use: ./lime $*" >&2
cd "$(dirname "$0")"
exec ./lime "$@"
